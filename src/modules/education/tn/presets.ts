/**
 * Presets تونسية: à enrichir au fur et à mesure (programmes officiels).
 * Ici on structure pour éviter de casser l'app.
 */
export const TN_PROGRAM_PRESETS = {
  lycee: {
    bac: {
      math: {
        units: ["Analyse", "Algèbre", "Probabilités", "Géométrie"]
      }
    }
  }
} as const;
